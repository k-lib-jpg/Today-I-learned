#ワークツリーの段階でのファイルへの変更を取り消す（--とつけているのはブランチ名とファイル名が被っても判別するため）

git checkout -- <ファイル名orディレクトリ名>

#全変更の取り消し

git checkout -- .

#ステージした変更を取り消す(ワークツリーには影響はない）

git reset HEAD <ファイル名orディレクトリ名）
git reset HEAD .

#直前のコミットをやり直す(すでにプッシュされたものはやり直せない。別の人がリモートリポジトリが変わる場合があるから。）

git commit --amend

