/* COUNT関数 */
SELECT COUNT(*)
  FROM Shohin;

/* SUM関数 */
SELECT SUM(hanbai_tanka)
  FROM Shohin;

/* Min/Max関数 */
SELECT MIN(hanbai_tanka) 
FROM Shohin;

/* Avg関数 */
SELECT AVG(hanbai_tanka) 
FROM Shohin;
