cdとはディレクトリの移動するコマンドを指す

ディレクトリからディレクトリに移動する
cd /引数（例:/etc）

ディレクトリの確認
pwd

相対パスでカレントディレクトリから移動する方法もある（/だったり、ディレクトリをすべて書かずに済む）
cd 引数

また/は子の階層を示し、..は親の階層を示す。~はホームディレクトリを言う。
→子の階層に行きたければ　cd または cd /,親の階層に行きたければ cd ..
