#1開始
console.log('1: 開始');
#3タイマー終了
setTimeout(() => {
  console.log('2: タイマー終了');
}, 1000);
#2終了
console.log('3: 終了');

#setTimeout() は非同期イベント。指定された時間の後、イベントキューにコールバック関数が入る
#console.log('3: 終了') はすぐに実行される（同期処理）
#イベントループが、同期処理が終わった後にキューの中身（setTimeoutの中）を処理する
